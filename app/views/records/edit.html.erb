<%= turbo_frame_tag "record-dialog" do %>
  <h1>Edit record</h1>
  <%= render "form", record: @record %>
  <button data-action="click->record-dialog#close"><%= t ".cancel_button" %></button>
<% end %>
